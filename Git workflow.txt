git git fetch
git pull origin main

~ to get new updates

gi fetch // new updates?
git pull origin main // pull new updates
git status	// checks for status of git repository
git add .        // or name of specific file
git commit -m "Commit message"
git push origin main

~ to push new additions

git branch name // new branch
git checkout name // switch branch

~ start working

git checkout oskar // switch to own branch
git merge main // merge with main branch
	